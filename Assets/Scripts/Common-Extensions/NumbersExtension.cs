using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class NumbersExtension
{
    /*
     * ToString de float
     *      Version que la coma sea un punto
     *      Version que la coma sea una coma
     *      puedo elegir la cantidad de decimales
     *
     * Regla de 3 simple
     * regla de 3 simple inversa
     * porcentage
     * porcentage inverso (se el porcentage que represta el numero, pero quiero calcular el 100)
     */
    
    
    public static string ToString(this float f, int n = 2, bool comma = true)
    {
        //retorne el float con la cantidad de numeros que le diga despues de la coma
        
        //f.ToString("")

        return string.Empty;
    }
}
